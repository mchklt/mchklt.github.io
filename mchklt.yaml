swagger: '2.0'
info:
  title: XSS BY MCHKLT"><h1>helloworld</h1>
  description: |
    "><h1>helloworld</h1>
  version: production
basePath: /JSSResource/
produces:
  - application/xml
  - application/json
consumes:
  - application/xml
  - application/json
security:
  - basicAuth: []
paths:
  /MCHKLT:
    get:
      responses:
        '200':
          description: No response was specified
      tags:
        - MCHKLT_XSS
      operationId: findAccounts
      summary: Finds all accounts
  '/hack/hachid/{id}':
    delete:
      parameters:
        - description: |
            "><h1>helloworld</h1>
          format: int64
          in: path
          name: id
          required: true
          type: integer
      responses:
        '200':
          description: No response was specified
